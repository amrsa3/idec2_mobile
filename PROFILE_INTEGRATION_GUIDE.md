# دليل تكامل صفحة الملف الشخصي

## التغييرات المطبقة

تم تحديث صفحة الملف الشخصي لتستخدم نظام القواعد الجديد (Profile Rules Integration) الذي يتحكم في إمكانية تعديل الحقول بناءً على حالة التحقق.

### الملفات المحدثة

1. **`lib/core/router/app_router.dart`**
   - تم استبدال `ProfileViewScreen` بـ `ProfileEditScreen`
   - الصفحة الجديدة تستخدم نظام القواعد المتكامل

2. **`lib/screens/profile/profile_edit_screen.dart`**
   - صفحة الملف الشخصي الجديدة
   - تستخدم `profileProvider` و `profileRulesProvider`
   - تعرض بانر حالة التحقق
   - تتحكم في إمكانية التعديل بناءً على القواعد

3. **`lib/widgets/profile/document_attach_button.dart`**
   - تم إصلاح استيراد `UserProfileExtended`
   - يعمل الآن بشكل صحيح مع النموذج الجديد

### المكونات الرئيسية

#### 1. ProfileEditScreen
الصفحة الرئيسية للملف الشخصي التي تعرض:
- بانر حالة التحقق (موثق، قيد المراجعة، مرفوض، غير موثق)
- الحقول القابلة للتعديل بناءً على حالة التحقق
- أزرار إرفاق الوثائق للحقول التي تتطلب ذلك
- رسالة تحذير عند الحفظ إذا كانت التعديلات تتطلب موافقة

#### 2. VerificationStatusBanner
بانر يعرض حالة التحقق الحالية:
- **غير موثق**: يظهر باللون الأحمر مع رسالة توضيحية
- **قيد المراجعة**: يظهر باللون الأصفر
- **موثق**: يظهر باللون الأخضر
- **مرفوض**: يظهر باللون الأحمر مع سبب الرفض

#### 3. EditableProfileField
حقل نص قابل للتعديل يتحكم في إمكانية التعديل بناءً على:
- حالة التحقق الحالية
- قواعد الحقل المحددة في النظام

#### 4. DocumentAttachButton
زر لإرفاق الوثائق المطلوبة:
- يظهر فقط للحقول التي تتطلب وثائق
- يسمح بالتقاط صورة أو اختيار من المعرض
- يعرض حالة الوثيقة المرفقة

### كيفية الاستخدام

1. **الوصول للصفحة**:
   ```dart
   context.go('/profile');
   ```

2. **تعديل الحقول**:
   - الحقول القابلة للتعديل تظهر بشكل نشط
   - الحقول غير القابلة للتعديل تظهر بشكل معطل

3. **إرفاق الوثائق**:
   - اضغط على زر "إرفاق وثيقة"
   - اختر المصدر (كاميرا أو معرض)
   - سيتم رفع الوثيقة تلقائياً

4. **حفظ التعديلات**:
   - اضغط على زر "حفظ التعديلات"
   - إذا كانت التعديلات تتطلب موافقة، سيظهر تحذير
   - قم بتأكيد الحفظ

### القواعد الافتراضية

- **غير موثق**: يمكن تعديل جميع الحقول
- **قيد المراجعة**: لا يمكن تعديل أي حقول
- **موثق**: لا يمكن تعديل الحقول الأساسية (الاسم، المؤهل، إلخ)
- **مرفوض**: يمكن تعديل الحقول المرفوضة فقط

### الملفات القديمة

الملفات التالية لم تعد مستخدمة:
- `lib/features/profile/presentation/profile_view_screen.dart` (القديمة)

يمكن حذفها أو الاحتفاظ بها كمرجع.

## الاختبار

للتأكد من أن كل شيء يعمل:

1. قم بتشغيل التطبيق
2. انتقل إلى صفحة الملف الشخصي
3. تحقق من:
   - ظهور بانر حالة التحقق
   - إمكانية تعديل الحقول بناءً على الحالة
   - عمل أزرار إرفاق الوثائق
   - ظهور رسالة التحذير عند الحفظ

## المشاكل المحتملة

إذا ظهرت الصفحة فارغة:
1. تأكد من أن `profileProvider` يحمل البيانات بشكل صحيح
2. تحقق من أن `profileRulesProvider` يحمل القواعد
3. راجع console للأخطاء

## الدعم

للمزيد من المعلومات، راجع:
- `.kiro/specs/profile-rules-integration/requirements.md`
- `.kiro/specs/profile-rules-integration/design.md`
- `.kiro/specs/profile-rules-integration/tasks.md`
