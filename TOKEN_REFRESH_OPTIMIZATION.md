# تحسين تجديد التوكن لتقليل الحمل على الخادم

## 🔍 المشكلة

الحل السابق كان يستخدم `Timer.periodic` كل 5 دقائق لجميع المستخدمين، مما يسبب:
- ❌ حمل غير ضروري على الخادم عند كثرة المستخدمين
- ❌ استهلاك بطارية أكثر
- ❌ طلبات متكررة غير ضرورية

### مثال على المشكلة:
- إذا كان لديك 1000 مستخدم نشط
- كل مستخدم يفحص كل 5 دقائق
- = 200 طلب/دقيقة = 12,000 طلب/ساعة (حتى لو لم يكن هناك حاجة للتجديد)

## ✅ الحل المحسّن

تم تحسين الحل لاستخدام **Timer محسوب** بناءً على وقت انتهاء التوكن الفعلي:

### المزايا:
1. ✅ **جدولة واحدة فقط** لكل مستخدم بناءً على وقت انتهاء التوكن
2. ✅ **لا فحوصات دورية** - فقط عند الحاجة الفعلية
3. ✅ **تقليل الحمل بشكل كبير** على الخادم
4. ✅ **توفير البطارية** - لا فحوصات مستمرة

### كيف يعمل:

```dart
// مثال: التوكن ينتهي بعد 15 دقيقة
// النظام يحسب: 15 دقيقة - 10 دقائق = 5 دقائق
// يتم جدولة تجديد واحد فقط بعد 5 دقائق
// بعد نجاح التجديد، يتم جدولة التالي
```

### الفرق:

#### الحل القديم:
```
المستخدم 1: فحص كل 5 دقائق → 12 فحص/ساعة
المستخدم 2: فحص كل 5 دقائق → 12 فحص/ساعة
...
المستخدم 1000: فحص كل 5 دقائق → 12 فحص/ساعة
-----------------------------------
الإجمالي: 12,000 طلب/ساعة (غير ضروري)
```

#### الحل الجديد:
```
المستخدم 1: تجديد واحد بعد 5 دقائق → 1 طلب عند الحاجة
المستخدم 2: تجديد واحد بعد 7 دقائق → 1 طلب عند الحاجة
...
المستخدم 1000: تجديد واحد بعد 3 دقائق → 1 طلب عند الحاجة
-----------------------------------
الإجمالي: 1000 طلب موزعة على 15 دقيقة = ~4000 طلب/ساعة
(أقل بـ 3 مرات!)
```

## 🔧 آلية العمل

### 1. عند حفظ التوكن:
```dart
await saveTokens(...);
// بعد الحفظ، يتم جدولة التجديد التالي
_scheduleNextTokenRefresh();
```

### 2. حساب وقت التجديد:
```dart
final timeUntilExpiry = expiry.difference(DateTime.now());
const refreshBuffer = Duration(minutes: 10); // قبل 10 دقائق
final refreshTime = timeUntilExpiry - refreshBuffer;

// جدولة تجديد واحد فقط
_sessionTimer = Timer(refreshTime, () {
  await refreshAccessToken();
  // بعد النجاح، جدول التالي
  _scheduleNextTokenRefresh();
});
```

### 3. معالجة الأخطاء:
- إذا فشل التجديد، يتم إعادة المحاولة بعد 5 دقائق
- إذا حدث خطأ في الجدولة، يتم استخدام fallback (فحص كل 10 دقائق)

## 📊 المقارنة

| المقياس | الحل القديم | الحل الجديد | التحسين |
|---------|-------------|-------------|----------|
| **طلبات/ساعة (1000 مستخدم)** | 12,000 | ~4,000 | **67% أقل** |
| **استهلاك البطارية** | عالي | منخفض | **تحسن كبير** |
| **الحمل على الخادم** | عالي | منخفض | **تحسن كبير** |
| **دقة التجديد** | جيدة | ممتازة | **أفضل** |

## 🛡️ الأمان

- ✅ الحل الآمن تماماً - لا يؤثر على أمان النظام
- ✅ يتم تجديد التوكن قبل 10 دقائق من انتهائه
- ✅ معالجة شاملة للأخطاء
- ✅ Fallback mechanism في حالة الخطأ

## ⚡ الأداء

### للخادم:
- **تقليل الحمل بنسبة 67%** عند كثرة المستخدمين
- توزيع الطلبات على الوقت بدلاً من تكرارها
- تقليل استهلاك الموارد

### للتطبيق:
- **توفير البطارية** - لا فحوصات مستمرة
- **استجابة أسرع** - لا انتظار للفحص الدوري
- **استخدام أقل للشبكة** - طلبات فقط عند الحاجة

## 🧪 الاختبار

### سيناريو الاختبار:
1. سجل دخول
2. تحقق من أنه تم جدولة تجديد واحد فقط
3. انتظر حتى وقت التجديد
4. تحقق من نجاح التجديد
5. تحقق من جدولة التالي

### النتائج المتوقعة:
- ✅ تجديد واحد فقط عند الحاجة
- ✅ لا فحوصات دورية غير ضرورية
- ✅ تجديد ناجح قبل 10 دقائق من الانتهاء

---

**تاريخ التحديث**: $(date)
**الإصدار**: 2.0.0 (محسّن)
**الحالة**: ✅ محسّن ومحسوب للاستخدام على نطاق واسع

