# اختبار سيناريوهات Token Refresh

## السيناريوهات المطلوبة للاختبار:

### ✅ السيناريو 1: Access Token صالح
- **الحالة**: Access token صالح (لم ينتهي بعد)
- **المتوقع**: 
  - يتم إرسال الطلب مع الـ token مباشرة
  - لا يتم تحديث الـ token
  - الطلب ينجح

### ✅ السيناريو 2: Access Token منتهي + Refresh Token صالح
- **الحالة**: Access token منتهي، Refresh token صالح (7 أيام)
- **المتوقع**:
  - `getValidAccessToken()` يكتشف أن الـ token منتهي
  - يحاول تحديثه تلقائياً باستخدام refresh token
  - إذا نجح، يتم إرسال الطلب مع الـ token الجديد
  - الطلب ينجح بدون 401 error

### ✅ السيناريو 3: Access Token منتهي + Refresh Token منتهي
- **الحالة**: Access token منتهي، Refresh token منتهي أيضاً
- **المتوقع**:
  - `getValidAccessToken()` يحاول تحديث الـ token لكن يفشل
  - في `onRequest`، يتم رفض الطلب بـ 401 فوراً (بدون إرسال للخادم)
  - `_handle401Error` يحاول تحديث الـ token مرة أخرى لكن يفشل
  - يتم إنهاء الجلسة (logout)

### ✅ السيناريو 4: Access Token منتهي + فشل التحديث (مشكلة شبكة)
- **الحالة**: Access token منتهي، Refresh token صالح، لكن تحديث الـ token فشل (مشكلة شبكة)
- **المتوقع**:
  - `getValidAccessToken()` يحاول تحديث الـ token لكن يفشل
  - في `onRequest`، يتم رفض الطلب بـ 401 فوراً
  - `_handle401Error` يحاول تحديث الـ token مرة أخرى
  - إذا نجح، يتم إعادة الطلب الأصلي تلقائياً
  - إذا فشل مرة أخرى، يتم إنهاء الجلسة

### ✅ السيناريو 5: طلب متعدد في نفس الوقت (Race Condition)
- **الحالة**: عدة طلبات في نفس الوقت، Access token منتهي
- **المتوقع**:
  - طلب واحد فقط يقوم بتحديث الـ token
  - الطلبات الأخرى تنتظر حتى ينتهي التحديث
  - جميع الطلبات تستخدم نفس الـ token الجديد

## نقاط التحقق:

1. ✅ لا يتم إرسال أي طلب بدون token
2. ✅ التحديث التلقائي يعمل عند الحاجة فقط
3. ✅ لا يوجد تحديث دوري (لا ثقل على التطبيق)
4. ✅ معالجة Race Conditions بشكل صحيح
5. ✅ إعادة الطلب تلقائياً بعد تحديث الـ token

